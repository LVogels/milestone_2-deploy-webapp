apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ms2-frontend-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lv-ms2-frontend-service
            port:
              number: 80
